// 페이지네이션 설정
export const PAGINATION = {
  posts: 4,
  logs: 11,
  recent: 4,
} as const;

// 이미지 설정
export const IMAGE = {
  fallback: "/jump.webp",
  defaultWidth: 800,
  defaultHeight: 300,
} as const;
